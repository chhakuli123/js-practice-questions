// 1. Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with there type.

//Answer

//variable declaration

let myname = "chhakuli";
let data = true;
let type;
let value = null;
let num = 400;

//displaying all the vallue with there type

console.log(`value of sami is ${myname} and it's type is ${typeof myname}`);
console.log(`value of sami is ${data} and it's type is ${typeof data}`);
console.log(`value of sami is ${type} and it's type is ${typeof type}`);
console.log(`value of sami is ${value} and it's type is ${typeof value}`);
console.log(`value of sami is ${num} and it's type is ${typeof num}`);

