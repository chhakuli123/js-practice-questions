// 7. Use lastIndexOf to determine the position of the last occurrence of a script.

let myarray = ["name", "string", "boolean", "script"]
let output = myarray.lastIndexOf("script")


console.log(output);